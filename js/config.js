/* ==================================================
   最強チームメーカー  config.js
   ゲームバランスの設定。数字を変えたいときはここだけ直す。
   ================================================== */
'use strict';

const CONFIG = {

  /* --- 集める人数 --- */
  TEAM_SIZE: 9,

  /* --- 「見送る」を使える回数 --- */
  PASS_LIMIT: 5,

  /* --- 選手の出やすさ（ポジションごとの重み） ---
     数字が大きいほど出やすい。合計はいくつでもよい（比率で決まる）。
     実データは投手の人数が圧倒的に多いので、そのままだと投手ばかり出る。
     ここで出現率を調整している。
     捕手をわざと少なめにして「捕手が来ない」状況を起こしている。 */
  POSITION_WEIGHTS: {
    '投': 22,
    '捕': 9,
    '一': 9,
    '二': 10,
    '三': 10,
    '遊': 10,
    '左': 10,
    '中': 10,
    '右': 10,
  },

  /* --- 同じ選手（名前が同じ人）は1回の勝負で2回出さない --- */
  NO_DUPLICATE_NAME: true,

  /* --- 保存に使う名前とバージョン ---
     バージョンを上げると、古い保存データは読み捨てられる。 */
  SAVE_KEY: 'saikyo-team-maker',
  SAVE_VERSION: 1,

  /* --- シェア用 --- */
  SHARE_TEXT: '最強チームメーカーでチームを作ってみた！',
  SITE_NAME: '最強チームメーカー',

  /* --- 広告 ---
     true にすると広告枠が表示される。初期版は false（枠ごと出ない）。 */
  ADS_ENABLED: false,
};
