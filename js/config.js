/* ==================================================
   最強チームメーカー  config.js
   ゲームバランスの設定。数字を変えたいときはここだけ直す。
   ================================================== */
'use strict';

const CONFIG = {

  /* --- 集める人数 ---
     投手1人＋野手9人＝10人。DH制を前提にした構成。 */
  TEAM_SIZE: 10,
  PITCHER_SLOTS: 1,    // 投手はこの人数ちょうど（増やしても減らしてもよい）
  FIELDER_SLOTS: 9,    // 野手はこの人数ちょうど

  /* --- 「見送る」を使える回数 --- */
  PASS_LIMIT: 5,

  /* --- 残りの枠がこの人数以下になったら、見送りを禁止する ---
     「あと2人」になったら必ず取る、という仕組み。
     10人そろえることを、見送り回数より優先させるためのルール。 */
  FORCE_PICK_WHEN_REMAINING: 2,

  /* --- 選手の出やすさ（ポジションごとの重み） ---
     数字が大きいほど出やすい。合計はいくつでもよい（比率で決まる）。
     実データは投手の人数が圧倒的に多いので、そのままだと投手ばかり出る。
     ここで出現率を調整している。
     投手は1人しか取れないので、投手を引いたあとは自動的に出なくなる。 */
  POSITION_WEIGHTS: {
    '投': 16,
    '捕': 10,
    '一': 9,
    '二': 10,
    '三': 10,
    '遊': 10,
    '左': 10,
    '中': 10,
    '右': 10,
  },

  /* --- 二刀流選手の出やすさ ---
     人数が少ないので、そのままだとほぼ出ない。
     これは「1人ぶん出すときに、二刀流を選ぶ確率」（0〜1）。 */
  TWOWAY_RATE: 0.025,

  /* --- 同じ選手（名前が同じ人）は1回の勝負で2回出さない --- */
  NO_DUPLICATE_NAME: true,

  /* --- シーズン --- */
  SEASON_GAMES: 143,      // 1シーズンの試合数
  LEAGUE_TEAMS: 7,        // 自分のチームを加えた数（既存6＋自分）
  PITCHER_ROSTER: 10,     // シーズン用に補充できる投手の人数（自分の1人を含む）

  /* --- 保存に使う名前とバージョン ---
     バージョンを上げると、古い保存データは読み捨てられる。 */
  SAVE_KEY: 'saikyo-team-maker',
  SAVE_VERSION: 2,

  /* --- シェア用 --- */
  SHARE_TEXT: '最強チームメーカーでチームを作ってみた！',
  SITE_NAME: '最強チームメーカー',

  /* --- 広告 ---
     true にすると広告枠が表示される。初期版は false（枠ごと出ない）。 */
  ADS_ENABLED: false,
};
